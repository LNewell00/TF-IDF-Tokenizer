## Goal: Using Java, build an app that will tokenize documents within a folder and output the tf-idf values.

* Implemented a tokenizer and TF-IDF scoring system for text analysis
* Built using Java and Common Crawl datasets; demonstrates understanding of information retrieval and NLP feature extraction
* Integrate BM25 ranking to improve document relevance scoring
* Query system for document retrieval
